
class BaseAPI

    constructor: (baseParams={}, baseURL='') ->
        # @@

    # API methods

    addSnippet: (flow, snippetType) ->
        # @@

    deleteSnippet: (flow, snippet) ->
        # @@

    editSnippet: (flow, snippet) ->
        # @@

    listSnippets: (flow) ->
        # @@

    listSnippetTypes: (flow) ->
        # @@

    makeSnippetGlobal: (flow, snippet) ->
        # @@

    makeSnippetLocal: (flow, snippet) ->
        # @@

    orderSnippets: (flow, snippets) ->
        # @@